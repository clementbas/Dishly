meta {
  name: Update Recipe
  type: http
  seq: 5
}

put {
  url: {{baseUrl}}/api/recipes/{{recipeId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "title": "Spaghetti Carbonara (Updated)",
    "servings": 6
  }
}
